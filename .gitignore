build/
compile_commands.json
*.o
*.d

# Core dumps. valgrind names its own vgcore.PID, which `core` does not
# match -- a 140MB dump from a crash test was committed before this line
# existed.
core
vgcore.*
*.core

# Generated test asset (regenerated by test/run.sh)
